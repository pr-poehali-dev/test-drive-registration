export interface FormData {
  name: string;
  phone: string;
  email: string;
  car: string;
  date: string;
}

export interface Car {
  name: string;
  image: string;
  price: string;
}

export interface Feature {
  icon: string;
  title: string;
  description: string;
  color: string;
}
